# Homepage Application

A self-hosted application dashboard providing a centralized view of services and applications deployed to the k3s cluster.

## Deployment Strategy

This application is deployed as a Helm chart with customizable values for configuration.

## Installation

See the workflow at `.github/workflows/homepage-upgrade.yml` for automated deployment.
