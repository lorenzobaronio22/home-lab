name: Homepage OCI Upgrade

on:
  push:
    branches:
      - main
    paths:
      - "03.k3s-cluster/99.apps/homepage/**"
      - ".github/workflows/homepage-oci-upgrade.yml"
  workflow_dispatch:

concurrency:
  group: homepage-oci-upgrade
  cancel-in-progress: false

jobs:
  upgrade:
    name: Upgrade homepage on oci k3s
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    env:
      RELEASE_NAME: homepage
      RELEASE_NAMESPACE: homepage
      CHART_PATH: ./03.k3s-cluster/99.apps/homepage
      OPERATOR_HOSTNAME: tailscale-operator-oci
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Validate required secrets
        env:
          TS_OAUTH_CLIENT_ID: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          TS_OAUTH_SECRET: ${{ secrets.TS_OAUTH_SECRET }}
        run: |
          set -euo pipefail
          for var in TS_OAUTH_CLIENT_ID TS_OAUTH_SECRET; do
            if [ -z "${!var}" ]; then
              echo "Missing required secret: $var" >&2
              exit 1
            fi
          done

      - name: Connect runner to tailnet
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Install kubectl
        uses: azure/setup-kubectl@v5

      - name: Install Helm
        uses: azure/setup-helm@v5

      - name: Configure kubeconfig
        run: |
          tailscale configure kubeconfig "$OPERATOR_HOSTNAME"

      - name: Validate cluster access
        run: kubectl get nodes

      - name: Validate rendered manifests
        run: |
          helm template "$RELEASE_NAME" "$CHART_PATH" \
            --namespace "$RELEASE_NAMESPACE" \
            --values "$CHART_PATH/values.yaml" > /tmp/homepage-oci-rendered.yaml

      - name: Upgrade release
        run: |
          set -euo pipefail
          helm upgrade --install "$RELEASE_NAME" "$CHART_PATH" \
            --namespace "$RELEASE_NAMESPACE" \
            --create-namespace \
            --values "$CHART_PATH/values.yaml" \
            --wait

      - name: Verify deployment rollout
        run: |
          kubectl -n "$RELEASE_NAMESPACE" rollout status deployment/homepage --timeout=5m

      - name: Verify namespace workloads
        run: |
          kubectl -n "$RELEASE_NAMESPACE" get pods