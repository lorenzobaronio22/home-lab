apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # HelmRelease lives in cnpg-system and reuses the cloudnative-pg
  # HelmRepository declared in ../cnpg-operator (same namespace).
  - helmrelease.yaml
  - objectstore.yaml
