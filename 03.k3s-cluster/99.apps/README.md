# Applications

This folder contains application manifests and Helm charts deployed to the `oci` k3s cluster.

Each subfolder represents a standalone application with its own Helm chart and deployment configuration.

## Prerequisites

Applications require networking setup (see `03.k3s-cluster/01.networking/`).